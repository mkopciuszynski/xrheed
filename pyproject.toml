[project]
name = "xrheed"
authors = [
    {name = "Marek Kopciuszynski", email = "mkopciuszynski@gmail.com"},
]
description = "An xarray-based toolkit for RHEED image analysis"
readme = "README.md"
requires-python = ">=3.12"
keywords = ["RHEED", "diffraction", "xarray", "analysis"]
license = "MIT"
license-files = ["LICENSE"]

classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Physics",
    "Intended Audience :: Science/Research",
]

dependencies = [
    "xarray>=0.18.2",
    "numpy>=1.21.0",
    "scipy>=1.7.0",
    "matplotlib>=3.4.2",
    "ruptures>=1.1.7",
    "tqdm>=4.62.0",
    "lmfit>=1.1.0",
    "dill>=0.3.4",
    "Pillow>=9.0.0",
]

dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/mkopciuszynski/xrheed"
Repository = "https://github.com/mkopciuszynski/xrheed"
Issues = "https://github.com/mkopciuszynski/xrheed/issues"

[build-system]
requires = [
    "setuptools>=80",
    "setuptools-scm>=10.2.3",
]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
    "sphinx",
    "myst-parser",
    "myst-nb",
    "nbsphinx",
    "sphinx-rtd-theme",
    "build",
]

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "dirty-tag"